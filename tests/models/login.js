const sinon = require('sinon');
const chain = require('chai');

const connection = require('../../models/connection');

describe('Faz login na aplicação', () => {
    const payloadLogin = {
        email: 'email@example.com',
        password: 'password',
    };

    before(async () => {
        // retorno esperado nesse teste
        const execute =
    })
})